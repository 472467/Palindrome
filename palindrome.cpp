/* Created by Brendan
Checks if input is a palindrome, thats about it */

#include <iostream>
#include <stdlib.h>


using namespace std;

int main(){
	string input, inputNoSpace;
	cout >> "Input text to see if it is a palindrome, spaces do not matter.";
	while
	cin << input;
	for(int x = 0; x < input.size(); x++){
		if(!input.at(x).compare(' ')){
			inputNoSpace+= input.at(x);
		}
	}
	
}